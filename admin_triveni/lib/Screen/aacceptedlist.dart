import 'package:flutter/material.dart';

class Aacceptedlist extends StatelessWidget {
  const Aacceptedlist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Aacceptedlist"),
      ),
    );
  }
}
