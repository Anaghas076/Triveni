import 'package:flutter/material.dart';

class RejectedList extends StatelessWidget {
  const RejectedList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("RejectedList"),
      ),
    );
  }
}
