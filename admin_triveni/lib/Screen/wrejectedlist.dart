import 'package:flutter/material.dart';

class Wrejectedlist extends StatelessWidget {
  const Wrejectedlist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Wrejectedlist"),
      ),
    );
  }
}
