import 'package:flutter/material.dart';

class Wacceptedlist extends StatelessWidget {
  const Wacceptedlist({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Wacceptedlist"),
      ),
    );
  }
}
